# Sword Interceptors Example

This example demonstrates interceptors in Sword for both web and Socket.IO controllers. It shows logging interceptors for incoming requests and socket connections.

## Running the Example

1. Navigate to this directory: `cd examples/interceptors`
2. Run the application: `cargo run`

The server runs on port 8081. Make HTTP requests or connect via SocketIO to see the logs.
